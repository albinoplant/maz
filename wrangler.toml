name = "maz"
compatibility_date = "2026-08-08"

# Workers Static Assets — use with Cloudflare's required Deploy command:
#   npx wrangler deploy
[assets]
directory = "./dist"
not_found_handling = "single-page-application"
